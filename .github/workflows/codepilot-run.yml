name: CodePilot Run

on:
  repository_dispatch:
    types: [codepilot_generate]

jobs:
  receive:
    runs-on: ubuntu-latest
    steps:
      - name: Receive request
        run: |
          echo "CodePilot repository dispatch received"
          echo "Issue: ${{ github.event.client_payload.issue_number }}"
